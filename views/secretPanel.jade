html
    head
        title Stocking Quest - Dev Panel
        script( src="js/routeTester.js" )
        script( src="js/routes.js" )
        
body( onload="addFields()" )
    p Hit send on a route to test. For lists, put in valid JSON. Don't use this unless you're Eli.
    div( id="urlField" )
    iframe( width=500 height=150 src="about:blank" id="resultsFrame" )
    
    div( id="routeFields" )